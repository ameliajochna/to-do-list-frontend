import React, { Component } from 'react';

const NavBar = ()=> {
    return (
        <nav className="navbar navbar-light bg-light">
        <b className="navbar-brand" href="#" style={{color:"green"}}>
            To do list
        </b>
        </nav>
    );
}

export default NavBar;