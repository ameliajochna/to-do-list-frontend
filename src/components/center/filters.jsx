import React from "react";

const Filters = ({ filterName, filterPriority }) => {
  return (
    <>
      <div className="filter-area">
        <h4 className="filter-title">Your searches and filters:</h4>
      </div>
    </>
  );
};

export default Filters;
